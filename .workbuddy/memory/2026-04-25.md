# 2026-04-25 日志

## Nora Android 项目更新

### 厚/薄集成打包完成
- **构建风味**：已配置 `bundled`（厚集成）和 `slim`（薄集成）
- **签名配置**：创建了 `nora-release.jks`，配置 release 签名
- **Release APK 已生成**：
  - `nora-bundled-release.apk`（34.45 MB）
  - `nora-slim-release.apk`（34.45 MB）
- **README 已更新**：添加了 APK 下载说明和构建命令

### 技术细节
- `MODEL_BUNDLED` BuildConfig 字段控制模型加载路径
- bundled 变体：优先从 `filesDir/models/` 加载（首次启动从 assets 提取）
- slim 变体：优先扫描 `/data/local/tmp/llama/`，降级到 SAF 文件选择器
- 内存问题：Release 构建需要 `GRADLE_OPTS=-Xmx4g`，建议分开构建

### GitHub 发布完成
- 清理 Gemma 模型，仅保留 Qwen3-0.6B
- nora-qwen-release.apk (461.7 MB) 已推送到 GitHub
- 配置 Git LFS

### 架构重构：移除 SetupScreen，模型自动加载
- Navigation.kt：移除 Setup 导航，模型加载移至 MainNavigation LaunchedEffect
- ChatViewModel：新增 engineState/isModelReady/modelError，监听 engine.loadState
- ChatScreen：全屏加载覆盖层（!isModelReady 时显示），模型状态自动从 engineState 推导
- 启动流程：MainNavigation → loadBundledModelIfNeeded() → 提取→扫描→加载→ChatScreen 解锁
- 崩溃修复：SetupViewModel 中 bundled 模式扫描后忘记调用 loadSelectedModel()，导致 llmModule=null 追踪 *.apk 和 *.pte 文件

### 🎉 Phase 1 Gate Passed — 数据持久化完整（19:31）
- **Phase 1 Step 7**: Instrument 测试 Gate — **23/23 tests passed ✅**
- 测试文件：AppLaunchTest(3) + BrandingTest(4) + ConversationTest(3) + DataRepositoryTest(7) + MessagePersistenceTest(3) + ThemeTest(3)
- **6 轮迭代修复**测试适配 ChatScreen 首页导航（Navigation.kt 不再经过 SetupScreen）
- 宪法+设计双审计 8/8 全绿
- Git commit: `1f55596`
- **Phase 1 正式 COMPLETE（7/7 Steps, 100%）**
- NEXT_STEP: Phase 2 — 安全屋主界面（Sanctuary）
